/** @type {import('tailwindcss').Config} */
export default {
  // darkMode: 'class' means dark styles activate when the <html> element has class="dark"
  // This is what lets our JS toggle button work
  darkMode: 'class',
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
      // auto-rows-[180px] won't work without this — Tailwind needs to know arbitrary values
      gridAutoRows: {
        'bento': '180px',
      }
    },
  },
  plugins: [],
}